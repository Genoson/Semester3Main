-- customer table required
-- needs to have a primary key and customer information (name, address, phone, email)

--vehicle table required
-- needs to have a primary key and vehicle information (make, model, year, VIN)

-- order table required
-- needs to have a primary key and order information (order_id, customer_id, vehicle_id, order_date, order_status, ...)

--order_detail table required
-- needs to have a primary key and order_detail information (order_id, service_id, part_quantity, part_price, ...)

--service_type table required
-- needs to have a primary key and service_type information (service_type_id, service_type_name, ...)